"""Runnable in-package guards (not the pytest suite — that lives in top-level tests/).

The only member today is `keyless_invariant`, the executable keyless guard wired
into the Done signal: `python -m bad_research.tests.keyless_invariant`.
"""
